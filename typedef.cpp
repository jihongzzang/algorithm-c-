#include <bits/stdc++.h>

using namespace std;

/** typedef를 통해 타입의 이름을 새로이 별칭으로 정의하고 실제 타입이름 대신
 * 별칭으로 사용할 수 있습니다.*/

/** typedef <타입> <별칭>*/
typedef int i;

int main() {
  i a = 1;

  cout << a << "\n";

  return 0;
}